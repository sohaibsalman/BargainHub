services:
  postgres:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=root123
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data # allows docker to store DB data outside docker to the file system
volumes:
  pgdata: